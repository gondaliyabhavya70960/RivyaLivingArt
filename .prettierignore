.next
node_modules
data/higgsfield
docs/requirements
*.md
# Vendored decoders (Phase 21): third-party files, copied verbatim from three@0.186.0
public/draco
public/basis

# Adapter fixtures (Phase 27): pages captured as a third party would serve them. One of them is
# deliberately malformed — an unclosed `<li>` before `</body>` is exactly what the contract suite
# needs an adapter to survive — and Prettier refuses to parse it, correctly. Formatting the rest
# would be worse than not: a fixture reformatted to be tidy is no longer a sample of what a real
# site sends, which is the only thing it is for.
tests/fixtures/scraper

# VENDORED THIRD-PARTY SKILLS — do not reformat.
#
# `.agents/skills/` holds five design skills installed with `npx skills add`; `.claude/skills/<name>`
# are symlinks into it. Reformatting them would rewrite somebody else's source, and every future
# update would then arrive as a diff against our formatting rather than against theirs. The
# first-party skills in `.claude/skills/` are NOT excluded.
.agents/
